"use client";
import Link from "next/link"; import { useEffect, useState } from "react"; import { getChecks } from "@/lib/genlayer"; import type { QuoteCheck } from "@/lib/types";
export default function Checks() { const [items,setItems]=useState<QuoteCheck[]>([]); const [error,setError]=useState(""); useEffect(()=>{void getChecks().then(setItems).catch(e=>setError(e.message));},[]); return <div><div className="flex items-end justify-between gap-4"><div><p className="text-xs uppercase tracking-[.25em] text-court-400">Open registry</p><h1 className="mt-2 font-display text-5xl text-court-100">Quote checks</h1></div><Link href="/checks/new" className="rounded-md bg-court-400 px-4 py-2 text-sm font-bold text-court-950">New check</Link></div>{error?<p className="mt-6 border border-red-400/50 p-4 text-red-200">{error}</p>:null}<div className="mt-8 grid gap-3">{items.map(i=><Link key={i.id} href={`/checks/${i.id}`} className="border border-court-400/25 bg-court-950/45 p-5 hover:border-court-400/70"><div className="flex justify-between gap-4"><h2 className="font-display text-2xl text-court-100">{i.title}</h2><span className="text-xs uppercase text-court-400">{i.status}</span></div><p className="mt-3 line-clamp-2 text-sm italic leading-6 text-court-200">“{i.quote}”</p><p className="mt-3 text-xs text-court-400">{i.verdict || "Awaiting review"}</p></Link>)}{!error&&!items.length?<div className="border border-dashed border-court-400/35 p-8 text-court-200">No checks yet. Start with a quote you can cite publicly.</div>:null}</div></div>; }
