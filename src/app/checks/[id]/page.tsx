"use client";
import { useParams } from "next/navigation"; import { useEffect, useState, type FormEvent } from "react"; import { explainError, getCheck, getCounterContext, identity, write } from "@/lib/genlayer"; import { gen, type CounterContext, type QuoteCheck } from "@/lib/types"; import { useWallet } from "@/lib/wallet";
export default function Detail() { const { id } = useParams<{id:string}>(); const wallet=useWallet(); const [item,setItem]=useState<QuoteCheck>(); const [context,setContext]=useState<CounterContext[]>([]); const [error,setError]=useState(""); const [busy,setBusy]=useState(false); const load=()=>Promise.all([getCheck(id),getCounterContext(id)]).then(([a,b])=>{setItem(a);setContext(b);}).catch(e=>setError(e.message)); useEffect(()=>{void load();},[id]); async function act(name:string,args:string[]=[]){try{setBusy(true);setError("");await write(identity(wallet.mode,wallet.address,wallet.privateKey),name,[id,...args]);await load();}catch(e){setError(explainError(e));}finally{setBusy(false);}} async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await act("submit_counter_context",[String(f.get("url")),String(f.get("hash")),String(f.get("note"))]);} if(!item)return <p className="text-court-200">Loading on-chain recordâ€¦</p>; return <div className="grid gap-6 lg:grid-cols-[1.35fr_.65fr]"><article className="border border-court-400/30 bg-court-950/45 p-6"><p className="text-xs uppercase tracking-[.25em] text-court-400">Quote check #{id} Â· {item.status}</p><h1 className="mt-3 font-display text-4xl text-court-100">{item.title}</h1><blockquote className="mt-7 border-l-2 border-court-400 pl-5 font-display text-2xl leading-9 text-court-100">â€œ{item.quote}â€</blockquote><dl className="mt-8 grid gap-5 text-sm"><div><dt className="text-court-400">Claimed meaning</dt><dd className="mt-1 leading-6 text-court-100">{item.claimed_meaning}</dd></div><div><dt className="text-court-400">Primary source</dt><dd className="mt-1 break-all text-court-100 underline"><a href={item.source_url} target="_blank">{item.source_url}</a></dd></div></dl>{item.reasoning?<div className="mt-8 border-t border-court-400/20 pt-5"><p className="text-xs uppercase tracking-[.2em] text-court-400">Consensus finding Â· {item.verdict} Â· {item.confidence_band}</p><p className="mt-3 leading-7 text-court-100">{item.reasoning}</p></div>:null}</article><aside className="space-y-4"><section className="border border-court-400/30 bg-court-950/45 p-5"><p className="text-xs uppercase tracking-[.2em] text-court-400">Bond</p><p className="mt-2 font-display text-4xl text-court-100">{gen(item.bond)} GEN</p><p className="mt-2 text-xs text-court-200">Settlement is on-chain after consensus.</p></section>{item.status==="open"?<><form onSubmit={add} className="grid gap-3 border border-court-400/30 bg-court-950/45 p-5"><h2 className="font-display text-2xl text-court-100">Add context</h2><input name="url" type="url" placeholder="Public context URL" required/><input name="hash" pattern="0x[0-9a-fA-F]{64}" placeholder="Context SHA-256" required/><textarea name="note" placeholder="Why this source changes the reading" required/><button disabled={!wallet.address||busy} className="rounded-md bg-court-400 px-4 py-2 font-bold text-court-950">Add source</button></form><button disabled={!wallet.address||busy} onClick={()=>void act("request_verdict")} className="w-full rounded-md border border-court-400/70 px-4 py-3 font-bold text-court-100">Request GenLayer verdict</button></>:null}{item.status==="undetermined"&&wallet.address?.toLowerCase()===item.submitter?<button disabled={busy} onClick={()=>void act("recover_undetermined")} className="w-full rounded-md bg-court-400 px-4 py-3 font-bold text-court-950">Recover bond</button>:null}<section className="border border-court-400/20 p-5"><h2 className="font-display text-xl text-court-100">Context sources</h2>{context.map((c,i)=><a className="mt-3 block break-all text-sm text-court-200 underline" target="_blank" href={c.url} key={i}>{c.note}</a>)}{!context.length?<p className="mt-3 text-sm text-court-400">No counter-context added.</p>:null}</section>{error?<p className="border border-red-400/50 p-3 text-sm text-red-200">{error}</p>:null}</aside></div>; }
